# Scripts

| Script | Purpose | Platform |
|---|---|---|
| `setup.ps1` | One-shot dev setup (install deps + run tests) | Windows |
| `setup.sh` | Same as above | macOS/Linux |

## Usage

```bash
# Windows
.\scripts\setup.ps1

# macOS/Linux
bash scripts/setup.sh
```
